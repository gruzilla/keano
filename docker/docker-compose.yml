version: '3'

services:
  webserver:
    image: nginx:alpine
    volumes:
      - ../map/:/usr/share/nginx/html
    environment:
      - VIRTUAL_HOST=decarbnow.space
    networks:
      - revproxy

networks:
  revproxy:
    external: true
